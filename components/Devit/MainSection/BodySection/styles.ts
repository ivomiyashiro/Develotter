import styled from 'styled-components';
import { theme } from 'styles/theme';

export const Div = styled.div`
  color: ${theme.white};
  font-size: .9rem;
  margin-bottom: .75em;
  margin-top: .25em;
  width: 100%;
`;
